name: sync_core
description: Deterministic synchronization and end-to-end cryptographic primitives for Nyla.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  cryptography: ^2.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

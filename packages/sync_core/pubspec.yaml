name: sync_core
description: Deterministic synchronization primitives shared by Nyla clients.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
